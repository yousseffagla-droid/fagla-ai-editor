import {TOOL_RISK_LEVELS} from './contracts.js';import {ValidationError} from '../errors/index.js';export function registerResearchTools({registry,searchProvider}){registry.register({name:'research.search',description:'Controlled approved research search',riskLevel:TOOL_RISK_LEVELS.LOW,requiredPermission:'RESEARCH',handler:async(input)=>{if(!input||typeof input.query!=='string'||!input.query.trim())throw new ValidationError('Research query is required');if(input.query.length>500)throw new ValidationError('Research query too long');const maxResults=Math.max(1,Math.min(10,Number(input.maxResults||5)));const r=await searchProvider.search({query:input.query.trim(),maxResults});if(!r||!Array.isArray(r.results))throw new ValidationError('Malformed search provider response');return{results:r.results.slice(0,maxResults)};}});}
